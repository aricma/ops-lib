# ops — .NET wrapper (C#)

Thin C# binding over the Rust `ops` core, generated with
[uniffi-rs](https://mozilla.github.io/uniffi-rs/) from the same UDL as
the Python wrapper.

- Build tooling: `uniffi-bindgen-cs`
- Package name TBD (NuGet)

Not implemented yet.
