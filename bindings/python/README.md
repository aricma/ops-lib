# ops — Python wrapper

Thin Python binding over the Rust `ops` core, generated with
[uniffi-rs](https://mozilla.github.io/uniffi-rs/) from a shared UDL
(shared with the .NET wrapper). Distributed as a wheel via maturin.

- Build tooling: `uniffi-bindgen`, `maturin`
- Package name TBD

Not implemented yet.
