# Byte-exact test fixtures must never be line-ending normalized:
# the writer comparison tests compare output against these files
# character for character, and git's CRLF conversion on Windows
# checkouts would break them.
tests/fixtures/** -text
